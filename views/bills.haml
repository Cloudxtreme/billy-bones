%table{style: 'border: 1px solid'}
  %tr
    %th Дата замера/оплаты
    %th Значение счётчика
    %th Стоимость
    %th Категория
  - @bills.each do |b|
    %tr
      %td= b.date
      %td= b.meter
      %td= b.cost
      %td
        %a{href: "/category/#{ b.category_id }"}= b.category.name
